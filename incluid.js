const words = ['spray', 'elite', 'exuberant', 'destruction', 'present'];

const result = words.filter((word) => word.includes('de'));

console.log(result);
// Expected output: Array ["exuberant", "destruction", "present"]
